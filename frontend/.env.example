# React app — public env vars must be prefixed with REACT_APP_
# The dev server proxies /api → http://localhost:5000 via package.json "proxy" field.
# No API URL env var is needed in development.

# Google reCAPTCHA v2 site key (public — safe to expose)
REACT_APP_RECAPTCHA_SITE_KEY=your_recaptcha_site_key
